{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Ae Script",
            "type": "shell",
            "command": "osascript -e 'tell application id \"com.adobe.aftereffects\" to activate DoScriptFile \"${file}\"'",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}